FROM python:3.10-slim

WORKDIR /app

# Python 의존성 설치
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt flask flask-cors

# 애플리케이션 코드 복사 (app.py 포함)
COPY *.py .

# 포트 노출
EXPOSE 5000

# 실행
CMD ["python", "app.py"]
